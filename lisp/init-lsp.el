;;; init-lsp.el --- LSP 配置（已禁用，因为感觉太慢）

;; 如果以后想重新启用，可以在这里配置 eglot
;; eglot 是 Emacs 30 自带的 LSP 客户端，轻量且好用
;; corfu 提供代码编辑时的弹窗补全（和 vertico 互补，vertico 管 minibuffer）

(provide 'init-lsp)
;;; init-lsp.el ends here
